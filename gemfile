# frozen_string_literal: true

source 'https://rubygems.org'

gem 'github-pages', group: :jekyll_plugins

# Remove specific jekyll version as github-pages gem will handle it
# Remove specific webrick version as github-pages gem will handle it
gem 'webrick'
